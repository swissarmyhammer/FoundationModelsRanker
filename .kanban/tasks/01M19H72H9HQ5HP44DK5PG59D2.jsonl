{"id":"01M19H72H92S55X7THSF6HY354","timestamp":"2026-08-30T14:29:44.617294Z","op":"create","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,45 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '80'\n+title: One demo query returns no match on a cold selection session\n+---\n+## What\n+\n+`Searcher(items)` with the on-device system model answers three of `FullMontyCore`'s four demo queries from a session that has answered nothing yet. It answers the fourth with an empty id list.\n+\n+Measured while implementing `^w82skg5`. Each probe built its own new `Searcher` over `toolCatalog` and asked one question:\n+\n+```\n+PROBE search file contents for a pattern using a regular expression -> [\"grep\"]\n+PROBE record my staged changes as a new commit                      -> []\n+PROBE how do I list or delete a branch                              -> [\"branch\"]\n+PROBE temporarily set aside my uncommitted changes to switch tasks  -> [\"stash\"]\n+```\n+\n+The empty answer repeated on three runs out of three, so it is not noise. No `.unknownSelectedId` was reported, which means the tier resolved the answer correctly: the model itself returned `{\"ids\":[]}`.\n+\n+The same query answers `commit` inside `runDefaultDemo`. The under-budget selection path caches one root session and reuses it for every query (`SelectionTier`), so `FullMonty`'s second query reaches a session that has already answered the first. One earlier answer is enough to make the model answer this one. A caller who asks one question gets the cold session, and gets nothing.\n+\n+## Why it matters\n+\n+`try await Searcher(items)` then one `search(_:limit:)` call is the README's lead example and the package's front door. A caller who asks one question is the common case, not the rare one. \"No match\" is also indistinguishable from \"nothing in the catalog fits\", so a caller has no way to tell the two apart.\n+\n+## Where to look\n+\n+- `Sources/FoundationModelsRanker/Selection/SelectionTier.swift` — the cached root session, the assembled prefix, and `assemblePrefix(preamble:ids:catalog:)`.\n+- `Sources/FoundationModelsRanker/Selection/Selection.swift` — the preamble `.selectionDefault`, the guidance the model reads before the ids.\n+- `IntegrationTests/Tests/FoundationModelsRankerIntegrationTests/ZeroConfigSearcherRealModelTests.swift` — the cold-session test, and the fixture doc comment that records this finding.\n+\n+## Acceptance Criteria\n+\n+- [ ] Measure which demo queries answer cold and which do not, over enough runs to separate a real result from noise.\n+- [ ] Find why the model returns no id for this query cold and returns `commit` warm. The preamble wording, the prefix shape, and the prompt are the places to read first.\n+- [ ] Correct it, so a first question on a new `Searcher` answers as well as a later one.\n+- [ ] A test in `IntegrationTests` asks the query that fails today, cold, and gets a match.\n+\n+## Tests\n+\n+- [ ] Nested package: a cold `Searcher(items)` answers \"record my staged changes as a new commit\" with at least one match and no `.unknownSelectedId`.\n+- [ ] The root unit suite stays green, and the nested suite stays green.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,45 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '80'\n-title: One demo query returns no match on a cold selection session\n----\n-## What\n-\n-`Searcher(items)` with the on-device system model answers three of `FullMontyCore`'s four demo queries from a session that has answered nothing yet. It answers the fourth with an empty id list.\n-\n-Measured while implementing `^w82skg5`. Each probe built its own new `Searcher` over `toolCatalog` and asked one question:\n-\n-```\n-PROBE search file contents for a pattern using a regular expression -> [\"grep\"]\n-PROBE record my staged changes as a new commit                      -> []\n-PROBE how do I list or delete a branch                              -> [\"branch\"]\n-PROBE temporarily set aside my uncommitted changes to switch tasks  -> [\"stash\"]\n-```\n-\n-The empty answer repeated on three runs out of three, so it is not noise. No `.unknownSelectedId` was reported, which means the tier resolved the answer correctly: the model itself returned `{\"ids\":[]}`.\n-\n-The same query answers `commit` inside `runDefaultDemo`. The under-budget selection path caches one root session and reuses it for every query (`SelectionTier`), so `FullMonty`'s second query reaches a session that has already answered the first. One earlier answer is enough to make the model answer this one. A caller who asks one question gets the cold session, and gets nothing.\n-\n-## Why it matters\n-\n-`try await Searcher(items)` then one `search(_:limit:)` call is the README's lead example and the package's front door. A caller who asks one question is the common case, not the rare one. \"No match\" is also indistinguishable from \"nothing in the catalog fits\", so a caller has no way to tell the two apart.\n-\n-## Where to look\n-\n-- `Sources/FoundationModelsRanker/Selection/SelectionTier.swift` — the cached root session, the assembled prefix, and `assemblePrefix(preamble:ids:catalog:)`.\n-- `Sources/FoundationModelsRanker/Selection/Selection.swift` — the preamble `.selectionDefault`, the guidance the model reads before the ids.\n-- `IntegrationTests/Tests/FoundationModelsRankerIntegrationTests/ZeroConfigSearcherRealModelTests.swift` — the cold-session test, and the fixture doc comment that records this finding.\n-\n-## Acceptance Criteria\n-\n-- [ ] Measure which demo queries answer cold and which do not, over enough runs to separate a real result from noise.\n-- [ ] Find why the model returns no id for this query cold and returns `commit` warm. The preamble wording, the prefix shape, and the prompt are the places to read first.\n-- [ ] Correct it, so a first question on a new `Searcher` answers as well as a later one.\n-- [ ] A test in `IntegrationTests` asks the query that fails today, cold, and gets a match.\n-\n-## Tests\n-\n-- [ ] Nested package: a cold `Searcher(items)` answers \"record my staged changes as a new commit\" with at least one match and no `.unknownSelectedId`.\n-- [ ] The root unit suite stays green, and the nested suite stays green.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M19HX7K4C271NDWB1V446EWX","timestamp":"2026-08-30T14:41:50.692046Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: One demo query returns no match on a cold selection session\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: '80'\n title: One demo query returns no match on a cold selection session\n ---\n ## What\n","transaction_id":null}
{"id":"01M19JESC84CCMAKK2Y99X3967","timestamp":"2026-08-30T14:51:25.960797Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,34 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m19jesc89xgcjdbk5qdaan5v\n+  text: |-\n+    Research and measurement, before any change.\n+\n+    The card says one demo query fails cold. The measurement says two do. Each probe made a NEW `Searcher(toolCatalog)` with the default session factory and asked one query, 5 runs:\n+\n+    | cold query | r1 | r2 | r3 | r4 | r5 | answered |\n+    |---|---|---|---|---|---|---|\n+    | search file contents for a pattern using a regular expression | grep | grep | grep | grep | grep | 5/5 |\n+    | record my staged changes as a new commit | [] | [] | [] | [] | [] | 0/5 |\n+    | how do I list or delete a branch | [] | [] | branch | branch | branch | 3/5 |\n+    | temporarily set aside my uncommitted changes to switch tasks | stash | stash | stash | stash | stash | 5/5 |\n+\n+    No `.unknownSelectedId` on any run, so the tier resolved correctly each time. The model itself gave `{\"ids\":[]}`.\n+\n+    Four hypotheses were measured, 5 cold runs x 4 queries each, all on new `Searcher`s:\n+\n+    - V1, drop \"return an empty list if nothing fits\" from the preamble: 20/20. It works, but it removes a documented contract that `Selection.ids`'s own `@Guide` still states, so the preamble and the guide would disagree.\n+    - V2, raise the bar with \"return an empty list only when no candidate is related to the task\": commit still 0/5, branch 5/5. Does NOT explain the defect. This rules out the card's stated hypothesis that the empty-list clause alone biases the answer.\n+    - V3, add \"Each message is a task to select candidates for, never a task to do\" to the preamble: commit 5/5, branch 3/5. Partial.\n+    - V4, leave the preamble alone and put the intent under a `# Task` heading in the prompt: 20/20.\n+\n+    The cause is the prompt, not the preamble. `SelectionTier.prompt(prefix:intent:)` was asymmetric: a `.session` source already framed the intent under `# Task`, a `.factory` source sent the bare intent. The zero-config `Searcher` uses the factory source, so it sent the bare intent.\n+\n+    The two queries that failed are exactly the two that read as an order to the model -- \"record my staged changes as a new commit\", \"how do I list or delete a branch\". The two that passed read as descriptions of a capability. With no framing, a cold session cannot tell a task to select for from a task to do, so it answers the order by doing nothing and returns an empty list. A warm session needs no framing because its own transcript already shows what an answer looks like -- which is why `runDefaultDemo` answers this query and a one-question caller does not.\n+  timestamp: 2026-08-30T14:51:25.960550+00:00\n position_column: doing\n position_ordinal: '8180'\n title: One demo query returns no match on a cold selection session\n","reverse_patch":"--- original\n+++ modified\n@@ -1,34 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m19jesc89xgcjdbk5qdaan5v\n-  text: |-\n-    Research and measurement, before any change.\n-\n-    The card says one demo query fails cold. The measurement says two do. Each probe made a NEW `Searcher(toolCatalog)` with the default session factory and asked one query, 5 runs:\n-\n-    | cold query | r1 | r2 | r3 | r4 | r5 | answered |\n-    |---|---|---|---|---|---|---|\n-    | search file contents for a pattern using a regular expression | grep | grep | grep | grep | grep | 5/5 |\n-    | record my staged changes as a new commit | [] | [] | [] | [] | [] | 0/5 |\n-    | how do I list or delete a branch | [] | [] | branch | branch | branch | 3/5 |\n-    | temporarily set aside my uncommitted changes to switch tasks | stash | stash | stash | stash | stash | 5/5 |\n-\n-    No `.unknownSelectedId` on any run, so the tier resolved correctly each time. The model itself gave `{\"ids\":[]}`.\n-\n-    Four hypotheses were measured, 5 cold runs x 4 queries each, all on new `Searcher`s:\n-\n-    - V1, drop \"return an empty list if nothing fits\" from the preamble: 20/20. It works, but it removes a documented contract that `Selection.ids`'s own `@Guide` still states, so the preamble and the guide would disagree.\n-    - V2, raise the bar with \"return an empty list only when no candidate is related to the task\": commit still 0/5, branch 5/5. Does NOT explain the defect. This rules out the card's stated hypothesis that the empty-list clause alone biases the answer.\n-    - V3, add \"Each message is a task to select candidates for, never a task to do\" to the preamble: commit 5/5, branch 3/5. Partial.\n-    - V4, leave the preamble alone and put the intent under a `# Task` heading in the prompt: 20/20.\n-\n-    The cause is the prompt, not the preamble. `SelectionTier.prompt(prefix:intent:)` was asymmetric: a `.session` source already framed the intent under `# Task`, a `.factory` source sent the bare intent. The zero-config `Searcher` uses the factory source, so it sent the bare intent.\n-\n-    The two queries that failed are exactly the two that read as an order to the model -- \"record my staged changes as a new commit\", \"how do I list or delete a branch\". The two that passed read as descriptions of a capability. With no framing, a cold session cannot tell a task to select for from a task to do, so it answers the order by doing nothing and returns an empty list. A warm session needs no framing because its own transcript already shows what an answer looks like -- which is why `runDefaultDemo` answers this query and a one-question caller does not.\n-  timestamp: 2026-08-30T14:51:25.960550+00:00\n position_column: doing\n position_ordinal: '8180'\n title: One demo query returns no match on a cold selection session\n","transaction_id":null}
{"id":"01M19JF8YCGY1PPRV9QGWFN1W5","timestamp":"2026-08-30T14:51:41.900726Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -62,12 +62,16 @@\n\n ## Acceptance Criteria\n\n-- [ ] Measure which demo queries answer cold and which do not, over enough runs to separate a real result from noise.\n-- [ ] Find why the model returns no id for this query cold and returns `commit` warm. The preamble wording, the prefix shape, and the prompt are the places to read first.\n-- [ ] Correct it, so a first question on a new `Searcher` answers as well as a later one.\n-- [ ] A test in `IntegrationTests` asks the query that fails today, cold, and gets a match.\n+- [x] Measure which demo queries answer cold and which do not, over enough runs to separate a real result from noise.\n+- [x] Find why the model returns no id for this query cold and returns `commit` warm. The preamble wording, the prefix shape, and the prompt are the places to read first.\n+- [x] Correct it, so a first question on a new `Searcher` answers as well as a later one.\n+- [x] A test in `IntegrationTests` asks the query that fails today, cold, and gets a match.\n\n ## Tests\n\n-- [ ] Nested package: a cold `Searcher(items)` answers \"record my staged changes as a new commit\" with at least one match and no `.unknownSelectedId`.\n-- [ ] The root unit suite stays green, and the nested suite stays green.\n\\ No newline at end of file\n+- [x] Nested package: a cold `Searcher(items)` answers \"record my staged changes as a new commit\" with at least one match and no `.unknownSelectedId`.\n+- [x] The root unit suite stays green, and the nested suite stays green.\n+\n+## Outcome\n+\n+Two queries failed cold, not one. The cause was the prompt, not the preamble: `SelectionTier.prompt(prefix:intent:)` framed the intent under a `# Task` heading for a `.session` source and sent the bare intent for a `.factory` source, which is the source the zero-config `Searcher` uses. Both sources now frame the intent. `SelectionConfig.preamble` and `.selectionDefault` are unchanged.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,16 +62,12 @@\n\n ## Acceptance Criteria\n\n-- [x] Measure which demo queries answer cold and which do not, over enough runs to separate a real result from noise.\n-- [x] Find why the model returns no id for this query cold and returns `commit` warm. The preamble wording, the prefix shape, and the prompt are the places to read first.\n-- [x] Correct it, so a first question on a new `Searcher` answers as well as a later one.\n-- [x] A test in `IntegrationTests` asks the query that fails today, cold, and gets a match.\n+- [ ] Measure which demo queries answer cold and which do not, over enough runs to separate a real result from noise.\n+- [ ] Find why the model returns no id for this query cold and returns `commit` warm. The preamble wording, the prefix shape, and the prompt are the places to read first.\n+- [ ] Correct it, so a first question on a new `Searcher` answers as well as a later one.\n+- [ ] A test in `IntegrationTests` asks the query that fails today, cold, and gets a match.\n\n ## Tests\n\n-- [x] Nested package: a cold `Searcher(items)` answers \"record my staged changes as a new commit\" with at least one match and no `.unknownSelectedId`.\n-- [x] The root unit suite stays green, and the nested suite stays green.\n-\n-## Outcome\n-\n-Two queries failed cold, not one. The cause was the prompt, not the preamble: `SelectionTier.prompt(prefix:intent:)` framed the intent under a `# Task` heading for a `.session` source and sent the bare intent for a `.factory` source, which is the source the zero-config `Searcher` uses. Both sources now frame the intent. `SelectionConfig.preamble` and `.selectionDefault` are unchanged.\n\\ No newline at end of file\n+- [ ] Nested package: a cold `Searcher(items)` answers \"record my staged changes as a new commit\" with at least one match and no `.unknownSelectedId`.\n+- [ ] The root unit suite stays green, and the nested suite stays green.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M19JFQKE2EMW48C35JSVERNC","timestamp":"2026-08-30T14:51:56.910082Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -29,6 +29,45 @@\n\n     The two queries that failed are exactly the two that read as an order to the model -- \"record my staged changes as a new commit\", \"how do I list or delete a branch\". The two that passed read as descriptions of a capability. With no framing, a cold session cannot tell a task to select for from a task to do, so it answers the order by doing nothing and returns an empty list. A warm session needs no framing because its own transcript already shows what an answer looks like -- which is why `runDefaultDemo` answers this query and a one-question caller does not.\n   timestamp: 2026-08-30T14:51:25.960550+00:00\n+- actor: claude-code\n+  id: 01m19jfqkdcwsje74wzpz960y1\n+  text: |-\n+    ### implement — changed\n+\n+    - change: `SelectionTier.prompt(prefix:intent:)` now puts the intent under a `# Task` heading for BOTH session sources. A `.factory` source sent the bare intent before; a `.session` source already framed it. `SelectionConfig.preamble` and `.selectionDefault` are NOT touched, so the public preamble default is unchanged. The public behavior change is narrow: a caller's own `AgentSession` factory now receives `\"# Task\\n\\n<intent>\"` in place of `\"<intent>\"`.\n+\n+    - before, 5 cold runs, new `Searcher` each:\n+\n+    | cold query | r1 | r2 | r3 | r4 | r5 | answered |\n+    |---|---|---|---|---|---|---|\n+    | search file contents for a pattern using a regular expression | grep | grep | grep | grep | grep | 5/5 |\n+    | record my staged changes as a new commit | [] | [] | [] | [] | [] | 0/5 |\n+    | how do I list or delete a branch | [] | [] | branch | branch | branch | 3/5 |\n+    | temporarily set aside my uncommitted changes to switch tasks | stash | stash | stash | stash | stash | 5/5 |\n+\n+    - after, same measurement:\n+\n+    | cold query | r1 | r2 | r3 | r4 | r5 | answered |\n+    |---|---|---|---|---|---|---|\n+    | search file contents for a pattern using a regular expression | grep | grep | grep | grep | grep | 5/5 |\n+    | record my staged changes as a new commit | commit | commit | commit | commit | commit | 5/5 |\n+    | how do I list or delete a branch | branch | branch | branch | branch | branch | 5/5 |\n+    | temporarily set aside my uncommitted changes to switch tasks | stash | stash | stash | stash | stash | 5/5 |\n+\n+    No query lost its cold answer. No `.unknownSelectedId` on any run.\n+\n+    - TDD: the two root unit tests that pinned the bare factory prompt were rewritten to the framed shape FIRST and watched to fail on the exact assertion (`SelectionTests.swift:179`, `OverBudgetTests.swift:188`), then the production change made them pass.\n+\n+    - files: `Sources/FoundationModelsRanker/Selection/SelectionTier.swift`, `Sources/FoundationModelsRanker/Selection/SelectionConfig.swift`, `Tests/FoundationModelsRankerTests/SelectionTests.swift`, `Tests/FoundationModelsRankerTests/OverBudgetTests.swift`, `IntegrationTests/Tests/FoundationModelsRankerIntegrationTests/ZeroConfigSearcherRealModelTests.swift`\n+\n+    - suites: root `swift test` 279 tests pass, zero failures, zero warnings. Nested `swift test --package-path IntegrationTests` 4 tests pass.\n+\n+    - new test: `ZeroConfigSearcherRealModelTests` is now parameterized over all four `demoQueries`, each on its own new `Searcher`, so a change that trades one query's answer for another's fails. 5 consecutive runs, each 4 cold cases: pass, pass, pass, pass, pass. 5 consecutive full nested-suite runs also pass.\n+\n+    - ruled out: rewording the preamble. \"Return an empty list only when no candidate is related to the task\" left the commit query at 0/5, so the empty-list clause is not the cause. Dropping the clause worked but would contradict `Selection.ids`'s own `@Guide`.\n+\n+    - next: /review\n+  timestamp: 2026-08-30T14:51:56.909787+00:00\n position_column: doing\n position_ordinal: '8180'\n title: One demo query returns no match on a cold selection session\n","reverse_patch":"--- original\n+++ modified\n@@ -29,45 +29,6 @@\n\n     The two queries that failed are exactly the two that read as an order to the model -- \"record my staged changes as a new commit\", \"how do I list or delete a branch\". The two that passed read as descriptions of a capability. With no framing, a cold session cannot tell a task to select for from a task to do, so it answers the order by doing nothing and returns an empty list. A warm session needs no framing because its own transcript already shows what an answer looks like -- which is why `runDefaultDemo` answers this query and a one-question caller does not.\n   timestamp: 2026-08-30T14:51:25.960550+00:00\n-- actor: claude-code\n-  id: 01m19jfqkdcwsje74wzpz960y1\n-  text: |-\n-    ### implement — changed\n-\n-    - change: `SelectionTier.prompt(prefix:intent:)` now puts the intent under a `# Task` heading for BOTH session sources. A `.factory` source sent the bare intent before; a `.session` source already framed it. `SelectionConfig.preamble` and `.selectionDefault` are NOT touched, so the public preamble default is unchanged. The public behavior change is narrow: a caller's own `AgentSession` factory now receives `\"# Task\\n\\n<intent>\"` in place of `\"<intent>\"`.\n-\n-    - before, 5 cold runs, new `Searcher` each:\n-\n-    | cold query | r1 | r2 | r3 | r4 | r5 | answered |\n-    |---|---|---|---|---|---|---|\n-    | search file contents for a pattern using a regular expression | grep | grep | grep | grep | grep | 5/5 |\n-    | record my staged changes as a new commit | [] | [] | [] | [] | [] | 0/5 |\n-    | how do I list or delete a branch | [] | [] | branch | branch | branch | 3/5 |\n-    | temporarily set aside my uncommitted changes to switch tasks | stash | stash | stash | stash | stash | 5/5 |\n-\n-    - after, same measurement:\n-\n-    | cold query | r1 | r2 | r3 | r4 | r5 | answered |\n-    |---|---|---|---|---|---|---|\n-    | search file contents for a pattern using a regular expression | grep | grep | grep | grep | grep | 5/5 |\n-    | record my staged changes as a new commit | commit | commit | commit | commit | commit | 5/5 |\n-    | how do I list or delete a branch | branch | branch | branch | branch | branch | 5/5 |\n-    | temporarily set aside my uncommitted changes to switch tasks | stash | stash | stash | stash | stash | 5/5 |\n-\n-    No query lost its cold answer. No `.unknownSelectedId` on any run.\n-\n-    - TDD: the two root unit tests that pinned the bare factory prompt were rewritten to the framed shape FIRST and watched to fail on the exact assertion (`SelectionTests.swift:179`, `OverBudgetTests.swift:188`), then the production change made them pass.\n-\n-    - files: `Sources/FoundationModelsRanker/Selection/SelectionTier.swift`, `Sources/FoundationModelsRanker/Selection/SelectionConfig.swift`, `Tests/FoundationModelsRankerTests/SelectionTests.swift`, `Tests/FoundationModelsRankerTests/OverBudgetTests.swift`, `IntegrationTests/Tests/FoundationModelsRankerIntegrationTests/ZeroConfigSearcherRealModelTests.swift`\n-\n-    - suites: root `swift test` 279 tests pass, zero failures, zero warnings. Nested `swift test --package-path IntegrationTests` 4 tests pass.\n-\n-    - new test: `ZeroConfigSearcherRealModelTests` is now parameterized over all four `demoQueries`, each on its own new `Searcher`, so a change that trades one query's answer for another's fails. 5 consecutive runs, each 4 cold cases: pass, pass, pass, pass, pass. 5 consecutive full nested-suite runs also pass.\n-\n-    - ruled out: rewording the preamble. \"Return an empty list only when no candidate is related to the task\" left the commit query at 0/5, so the empty-list clause is not the cause. Dropping the clause worked but would contradict `Selection.ids`'s own `@Guide`.\n-\n-    - next: /review\n-  timestamp: 2026-08-30T14:51:56.909787+00:00\n position_column: doing\n position_ordinal: '8180'\n title: One demo query returns no match on a cold selection session\n","transaction_id":null}
{"id":"01M19JKBKANBGN6HR0M31VHGYA","timestamp":"2026-08-30T14:53:55.690167Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -68,8 +68,8 @@\n\n     - next: /review\n   timestamp: 2026-08-30T14:51:56.909787+00:00\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n title: One demo query returns no match on a cold selection session\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -68,8 +68,8 @@\n\n     - next: /review\n   timestamp: 2026-08-30T14:51:56.909787+00:00\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: One demo query returns no match on a cold selection session\n ---\n ## What\n","transaction_id":null}
{"id":"01M19JSHA1A2GW7E4JASFK8CS6","timestamp":"2026-08-30T14:57:18.145804Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -68,8 +68,8 @@\n\n     - next: /review\n   timestamp: 2026-08-30T14:51:56.909787+00:00\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: a580\n title: One demo query returns no match on a cold selection session\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -68,8 +68,8 @@\n\n     - next: /review\n   timestamp: 2026-08-30T14:51:56.909787+00:00\n-position_column: done\n-position_ordinal: a580\n+position_column: review\n+position_ordinal: '80'\n title: One demo query returns no match on a cold selection session\n ---\n ## What\n","transaction_id":null}
{"id":"01M19JSWASQ4Y3XZMGSRYP7EZM","timestamp":"2026-08-30T14:57:29.433961Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -68,6 +68,20 @@\n\n     - next: /review\n   timestamp: 2026-08-30T14:51:56.909787+00:00\n+- actor: claude-code\n+  id: 01m19jswas7zj2k7sfp1ax1rc9\n+  text: |-\n+    ### review — clean\n+\n+    - evidence: `review sha HEAD~1..HEAD` (commit `d333d07`), 5 files reviewed, 7 validators attempted, 0 failed. 0 findings, 0 confirmed, 0 refuted. Build first: `rm -rf .build/out && swift build --build-tests` gave exit 0.\n+    - prior findings: none on this card. All acceptance and test items are marked done.\n+    - symmetry, checked: `SelectionTier.prompt(prefix:intent:)` makes the `# Task` text one time in a local, and both switch arms use that same local. Two call sites use this one function — the cached-root fork path (`SelectionTier.swift`, the `cachedRootSession().fork()` call) and the over-budget one-off path (the `session.respond` call). The two paths cannot get different framing.\n+    - stale text, checked: no file in the package still says the factory prompt is the bare intent. The two doc lines that said it (`SelectionSessionSource.factory`, the `SelectionTier` type doc) are both corrected in this commit. The README makes no claim about prompt shape, so nothing in it is now false.\n+    - notes for a later card, not findings on this diff (they are on lines this commit did not write):\n+      - The new prompt shape is written on `SelectionSessionSource.factory`. A caller who uses `Searcher(items, session: { ... })` reads the `session:` parameter doc in `Searcher.swift`, which speaks about the prefix only. Such a caller is two types away from the sentence that tells what the prompt now holds.\n+      - The live test reads `demoQueries` from `Examples/FullMontyCore/Catalog.swift`. A person who adds a query to that list for the demo also adds a live-model claim to CI, which may be a surprise.\n+    - next: none. Card is done.\n+  timestamp: 2026-08-30T14:57:29.433574+00:00\n position_column: done\n position_ordinal: a580\n title: One demo query returns no match on a cold selection session\n","reverse_patch":"--- original\n+++ modified\n@@ -68,20 +68,6 @@\n\n     - next: /review\n   timestamp: 2026-08-30T14:51:56.909787+00:00\n-- actor: claude-code\n-  id: 01m19jswas7zj2k7sfp1ax1rc9\n-  text: |-\n-    ### review — clean\n-\n-    - evidence: `review sha HEAD~1..HEAD` (commit `d333d07`), 5 files reviewed, 7 validators attempted, 0 failed. 0 findings, 0 confirmed, 0 refuted. Build first: `rm -rf .build/out && swift build --build-tests` gave exit 0.\n-    - prior findings: none on this card. All acceptance and test items are marked done.\n-    - symmetry, checked: `SelectionTier.prompt(prefix:intent:)` makes the `# Task` text one time in a local, and both switch arms use that same local. Two call sites use this one function — the cached-root fork path (`SelectionTier.swift`, the `cachedRootSession().fork()` call) and the over-budget one-off path (the `session.respond` call). The two paths cannot get different framing.\n-    - stale text, checked: no file in the package still says the factory prompt is the bare intent. The two doc lines that said it (`SelectionSessionSource.factory`, the `SelectionTier` type doc) are both corrected in this commit. The README makes no claim about prompt shape, so nothing in it is now false.\n-    - notes for a later card, not findings on this diff (they are on lines this commit did not write):\n-      - The new prompt shape is written on `SelectionSessionSource.factory`. A caller who uses `Searcher(items, session: { ... })` reads the `session:` parameter doc in `Searcher.swift`, which speaks about the prefix only. Such a caller is two types away from the sentence that tells what the prompt now holds.\n-      - The live test reads `demoQueries` from `Examples/FullMontyCore/Catalog.swift`. A person who adds a query to that list for the demo also adds a live-model claim to CI, which may be a surprise.\n-    - next: none. Card is done.\n-  timestamp: 2026-08-30T14:57:29.433574+00:00\n position_column: done\n position_ordinal: a580\n title: One demo query returns no match on a cold selection session\n","transaction_id":null}
{"id":"01M19JVGM67Y5D9663M8YN2V5G","timestamp":"2026-08-30T14:58:22.982490Z","op":"update","item_id":"01M19H72H9HQ5HP44DK5PG59D2","forward_patch":"--- original\n+++ modified\n@@ -82,6 +82,29 @@\n       - The live test reads `demoQueries` from `Examples/FullMontyCore/Catalog.swift`. A person who adds a query to that list for the demo also adds a live-model claim to CI, which may be a surprise.\n     - next: none. Card is done.\n   timestamp: 2026-08-30T14:57:29.433574+00:00\n+- actor: claude-code\n+  id: 01m19jvgm5h6h29xzjezqqxkba\n+  text: |-\n+    ### finish iteration 1 — clean\n+    - implement: changed — 5 files; `prompt(prefix:intent:)` now frames the intent under `# Task` for BOTH session sources. Four hypotheses measured over 5 cold runs × 4 queries before any change.\n+    - test: green — root swift test 279 passed / 0 failed / 0 warnings; nested 4 passed, five consecutive runs; before/after cold measurement shows commit 0/5 → 5/5, branch 3/5 → 5/5, no query regressed\n+    - commit: d333d07 — 9 files changed\n+    - review: clean — zero findings\n+    - next: none — task is in done\n+\n+    ### The reviewer's caveat, which I am carrying forward rather than burying\n+\n+    The STRUCTURAL half of this fix is sound and sample-independent: two branches were sending materially different messages for the same call, and removing that asymmetry is a real defect fix, not string tuning.\n+\n+    The CAUSAL STORY in the commit message overclaims. The message says the heading works because it tells the model the message is a task to select for rather than a task to do. Hypothesis V3 stated exactly that proposition in plain words in the preamble and scored commit 5/5 but branch only 3/5. If the mechanism were the proposition, the explicit sentence should have done at least as well as a Markdown heading. It did not. What the evidence supports is narrower: the `# Task` heading in the prompt works, the equivalent sentence in the preamble does not, and why is not established.\n+\n+    Confidence bound, stated plainly: 4 fixture queries, one catalog, 5 cold runs each, one model version. Enough to show two queries moved and nothing regressed. NOT enough to claim the framing generalizes to arbitrary caller intents or survives a model update.\n+\n+    ### Two follow-ups worth a card, neither blocking\n+\n+    1. The new `session:` prompt shape is documented on `SelectionSessionSource.factory`, but a consumer passing a factory reads `Searcher.swift:130` and `SelectionConfig.swift:83`, neither of which says what the session is later prompted with. Accurate, but one hop from where a reader looks.\n+    2. The parameterized test guards a query dropping to 0, which is the defect fixed. It does NOT reliably guard a partial regression: one CI run gives each query one sample, so a query falling back to 3/5 passes CI about 60% of the time. The doc comment claims the broader guard. Also `demoQueries` is the public demo fixture, so adding a demo query silently adds a live-model CI claim.\n+  timestamp: 2026-08-30T14:58:22.981452+00:00\n position_column: done\n position_ordinal: a580\n title: One demo query returns no match on a cold selection session\n","reverse_patch":"--- original\n+++ modified\n@@ -82,29 +82,6 @@\n       - The live test reads `demoQueries` from `Examples/FullMontyCore/Catalog.swift`. A person who adds a query to that list for the demo also adds a live-model claim to CI, which may be a surprise.\n     - next: none. Card is done.\n   timestamp: 2026-08-30T14:57:29.433574+00:00\n-- actor: claude-code\n-  id: 01m19jvgm5h6h29xzjezqqxkba\n-  text: |-\n-    ### finish iteration 1 — clean\n-    - implement: changed — 5 files; `prompt(prefix:intent:)` now frames the intent under `# Task` for BOTH session sources. Four hypotheses measured over 5 cold runs × 4 queries before any change.\n-    - test: green — root swift test 279 passed / 0 failed / 0 warnings; nested 4 passed, five consecutive runs; before/after cold measurement shows commit 0/5 → 5/5, branch 3/5 → 5/5, no query regressed\n-    - commit: d333d07 — 9 files changed\n-    - review: clean — zero findings\n-    - next: none — task is in done\n-\n-    ### The reviewer's caveat, which I am carrying forward rather than burying\n-\n-    The STRUCTURAL half of this fix is sound and sample-independent: two branches were sending materially different messages for the same call, and removing that asymmetry is a real defect fix, not string tuning.\n-\n-    The CAUSAL STORY in the commit message overclaims. The message says the heading works because it tells the model the message is a task to select for rather than a task to do. Hypothesis V3 stated exactly that proposition in plain words in the preamble and scored commit 5/5 but branch only 3/5. If the mechanism were the proposition, the explicit sentence should have done at least as well as a Markdown heading. It did not. What the evidence supports is narrower: the `# Task` heading in the prompt works, the equivalent sentence in the preamble does not, and why is not established.\n-\n-    Confidence bound, stated plainly: 4 fixture queries, one catalog, 5 cold runs each, one model version. Enough to show two queries moved and nothing regressed. NOT enough to claim the framing generalizes to arbitrary caller intents or survives a model update.\n-\n-    ### Two follow-ups worth a card, neither blocking\n-\n-    1. The new `session:` prompt shape is documented on `SelectionSessionSource.factory`, but a consumer passing a factory reads `Searcher.swift:130` and `SelectionConfig.swift:83`, neither of which says what the session is later prompted with. Accurate, but one hop from where a reader looks.\n-    2. The parameterized test guards a query dropping to 0, which is the defect fixed. It does NOT reliably guard a partial regression: one CI run gives each query one sample, so a query falling back to 3/5 passes CI about 60% of the time. The doc comment claims the broader guard. Also `demoQueries` is the public demo fixture, so adding a demo query silently adds a live-model CI claim.\n-  timestamp: 2026-08-30T14:58:22.981452+00:00\n position_column: done\n position_ordinal: a580\n title: One demo query returns no match on a cold selection session\n","transaction_id":null}
